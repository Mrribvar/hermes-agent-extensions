# DEPENDENCIES INVENTORY (Read-Only; Non-Secret; Non-Mutation)
Hermes package version: 1.0.0 (from package.json — non-sensitive)
Python system version: 3.14.6 (system python)
Python version file (.python-version): exists (non-sensitive metadata)
Dependencies: package.json + setup.py + pyproject.toml + uv.lock + flake.nix exist (non-sensitive metadata only; no secret content embedded)
SDK dependency: freestyle@0.2.13 (installed in node_modules; adapter uses existing binary; no new installation required for adapter)
Credentials for adapter: FREESTYLE_API_KEY (loaded from .env securely; masked [REDACTED_67_CHARS]; never shown; never embedded in adapter files/reports)
No secret embedded; no mutation performed; adapter layer independent from core dependency installation.
